meta {
  name: Log In Again
  type: http
  seq: 12
}

post {
  url: {{baseUrl}}/admin/user/session
  body: json
  auth: none
}

body:json {
  {
    "email": "{{testUserEmail}}",
    "password": "{{testUserPassword}}"
  }
}

assert {
  res.status: eq 200
}
