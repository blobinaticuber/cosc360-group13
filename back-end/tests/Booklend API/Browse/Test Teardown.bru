meta {
  name: Test Teardown
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/testing/teardown
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
