meta {
  name: Teardown
  type: http
  seq: 6
}

delete {
  url: {{baseUrl}}/testing/teardown
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
