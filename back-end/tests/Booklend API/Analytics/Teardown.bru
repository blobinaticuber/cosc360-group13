meta {
  name: Teardown
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/testing/teardown
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
