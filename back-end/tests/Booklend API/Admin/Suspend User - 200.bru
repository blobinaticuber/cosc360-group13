meta {
  name: Suspend User - 200
  type: http
  seq: 3
}

delete {
  url: {{baseUrl}}/admin/suspend/{{testData.users[0].id}}
  body: none
  auth: inherit
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
