meta {
  name: Delete Listing - 200
  type: http
  seq: 7
}

delete {
  url: {{baseUrl}}/listing/:id
  body: none
  auth: inherit
}

params:path {
  id: {{testListingId}}
}

assert {
  res.status: eq 200
}

settings {
  encodeUrl: true
  timeout: 0
}
