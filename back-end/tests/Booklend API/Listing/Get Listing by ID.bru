meta {
  name: Get Listing by ID
  type: http
  seq: 8
}

get {
  url: {{baseUrl}}/listing/:id
  body: none
  auth: inherit
}

params:path {
  id: {{testListingId}}
}

assert {
  res.status: eq 404
}

settings {
  encodeUrl: true
  timeout: 0
}
