meta {
  name: Register - 201
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/user
  body: json
  auth: none
}

body:json {
  {
    "name": "{{testUserName}}",
    "email": "{{testUserEmail}}",
    "password": "{{testUserPassword}}"
  }
  
}

assert {
  res.status: eq 201
}
